grpc:
  server:
    port: 0
    reflection-service-enabled: true

management:
  endpoints:
    web:
      exposure:
        include: gateway,health,info
  endpoint:
    gateway:
      enabled: true

server:
  port: 0

logging:
  level:
    ru.yandex.practicum: DEBUG

spring:
  kafka:
    bootstrap-servers: localhost:9092
  topics:
    user: 'stats.user-actions.v1'
    events: 'stats.events-similarity.v1'